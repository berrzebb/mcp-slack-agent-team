# Slack MCP Server

Claude Code ↔ 사용자 간 Slack 기반 양방향 커뮤니케이션 MCP 서버.

Slack을 통해 Claude Code 에이전트를 원격으로 제어하고, 멀티 에이전트 팀을 관리하며, 위험한 명령에 대해 Slack 기반 승인 워크플로우를 수행할 수 있습니다.

## 주요 기능

- **원격 제어**: Slack 채널에서 Claude Code에게 명령을 보내고, 결과를 스레드로 받기
- **명령 루프**: `slack_command_loop`로 채팅 인터페이스를 완전히 대체
- **긴 메시지 자동 처리**: 3,900자 초과 시 자동 분할, 8,000자 초과 시 파일 업로드
- **에이전트 팀 관리**: 전용 채널 생성, 역할별 이름/아이콘 표시, 브로드캐스트
- **승인 훅**: 위험 명령(git push, rm 등) 실행 전 Slack에서 승인 요청
- **세션 복구**: compact/재시작 후 상태를 자동 복원

## 프로젝트 구조

```
├── src/
│   ├── index.ts               # MCP 서버 본체 (21개 도구)
│   ├── approval-hook.ts       # 위험 명령 Slack 승인 훅
│   ├── test.ts                # 연결 테스트 스크립트
│   └── check.ts               # 간단한 연결 확인
├── package.json
├── tsconfig.json
└── .env.example
```

## 워크플로우

```
사용자 (Slack)             Agent (Claude Code)
    │                            │
    ├─── 명령 입력 ────────────→│  slack_read_messages / slack_command_loop
    │                            ├── 수신 확인 리액션 (👀)
    │                            ├── 작업 수행
    │                            ├── 결과 전송 (slack_reply_thread)
    │←── 결과 수신 ────────────┤  완료 리액션 (✅)
    ├─── 피드백 ──────────────→│  slack_wait_for_reply
    │                            ├── 피드백 반영
    │                            └── 다음 명령 대기 (루프)
```

## 빠른 시작

### 1. 설치

```bash
git clone https://github.com/berrzebb/mcp-slack-agent-team.git
cd mcp-slack-agent-team/mcp-servers/slack
npm install
npm run build
```

### 2. Slack App 생성 및 설정

1. [Slack API](https://api.slack.com/apps)에서 새 App을 생성합니다.
2. **OAuth & Permissions → Bot Token Scopes**에서 아래 [필요 권한](#필요한-bot-token-scopes)을 추가합니다.
3. App을 워크스페이스에 설치하고 **Bot User OAuth Token** (`xoxb-...`)을 복사합니다.
4. Slack에서 봇을 사용할 채널에 `/invite @봇이름`으로 봇을 초대합니다.
5. 해당 채널의 **채널 ID**를 확인합니다 (채널 이름 우클릭 → 링크 복사 → 마지막 경로).

### 3. 연결 테스트

```bash
# .env 파일 생성
cp .env.example .env
# .env 파일에 실제 토큰과 채널 ID 입력 후:

npx tsx src/test.ts
```

테스트가 통과하면 다음 출력이 나타납니다:

```
═══════════════════════════════════════
  Slack MCP Server - 연결 테스트
═══════════════════════════════════════

1️⃣  Bot 인증 확인...       ✅ 인증 성공
2️⃣  채널 접근 확인...       ✅ 채널: #your-channel
3️⃣  메시지 전송 테스트...   ✅ 메시지 전송 성공
4️⃣  메시지 읽기 테스트...   ✅ 최근 3개 메시지 읽기 성공
5️⃣  리액션 테스트...        ✅ 리액션 추가 성공

═══════════════════════════════════════
  ✅ 모든 테스트 통과!
═══════════════════════════════════════
```

### 4. Claude Code MCP 서버 등록

프로젝트의 `.claude/settings.json` 또는 사용자 전역 설정 `~/.claude.json`에 추가합니다:

```json
{
  "mcpServers": {
    "slack": {
      "command": "node",
      "args": ["path/to/slack-mcp-server/dist/index.js"],
      "env": {
        "SLACK_BOT_TOKEN": "xoxb-your-bot-token",
        "SLACK_DEFAULT_CHANNEL": "C채널ID"
      }
    }
  }
}
```

> **개발 중**에는 빌드 없이 `tsx`로 직접 실행 가능:
> ```json
> {
>   "mcpServers": {
>     "slack": {
>       "command": "npx",
>       "args": ["tsx", "path/to/slack-mcp-server/src/index.ts"],
>       "env": {
>         "SLACK_BOT_TOKEN": "xoxb-your-bot-token",
>         "SLACK_DEFAULT_CHANNEL": "C채널ID"
>       }
>     }
>   }
> }
> ```

### 5. (선택) 승인 훅 설정

위험한 명령(`git push`, `rm`, `git reset` 등)을 실행하기 전에 Slack에서 사용자 승인을 요청하는 훅입니다.

`.claude/settings.json`에 추가:

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node path/to/slack-mcp-server/dist/approval-hook.js"
          }
        ]
      }
    ]
  }
}
```

위험 명령 감지 시 Slack 채널에 승인 요청이 전송됩니다:

- ✅ 리액션 또는 스레드에 `y` → **승인** (명령 실행)
- ❌ 리액션 또는 스레드에 `n` → **거부** (명령 차단)
- 타임아웃 (기본 120초) → **자동 거부**

## 필요한 Bot Token Scopes

Slack App → OAuth & Permissions → Bot Token Scopes:

| Scope | 용도 | 필요 기능 |
|-------|------|-----------|
| `chat:write` | 메시지 전송 | 전체 |
| `chat:write.customize` | 에이전트 역할별 이름/아이콘으로 전송 | 팀 관리 |
| `channels:history` | 공개 채널 메시지 읽기 | 전체 |
| `groups:history` | 비공개 채널 메시지 읽기 | 비공개 채널 사용 시 |
| `reactions:write` | 리액션 추가 (수신 확인 👀, 완료 ✅) | 전체 |
| `reactions:read` | 리액션 읽기 (승인 훅의 승인/거부 확인) | 승인 훅 |
| `channels:read` | 공개 채널 목록 조회 | `slack_list_channels` |
| `groups:read` | 비공개 채널 목록 조회 | `slack_list_channels` |
| `channels:manage` | 채널 생성, 주제 설정, 아카이브 | 팀 관리 |
| `channels:join` | 채널 자동 참가 | 팀 관리 |
| `users:read` | 사용자 정보 조회 | 봇 ID 자동 감지 |
| `files:write` | 코드/로그 파일 업로드 | `slack_upload_snippet`, `slack_send_code`, 긴 메시지 자동 업로드 |

> **최소 구성** (기본 통신만): `chat:write`, `channels:history`, `reactions:write`, `users:read`
>
> **팀 관리 추가**: + `chat:write.customize`, `channels:manage`, `channels:join`
>
> **승인 훅 추가**: + `reactions:read`

## 제공 도구 (Tools)

### 기본 커뮤니케이션

| 도구 | 설명 |
|------|------|
| `slack_send_message` | 채널에 메시지 전송 (긴 메시지 자동 분할/파일 업로드) |
| `slack_read_messages` | 채널의 최근 메시지 읽기 |
| `slack_reply_thread` | 스레드에 답장 |
| `slack_wait_for_reply` | 새 메시지/답장 대기 (polling) |
| `slack_add_reaction` | 이모지 리액션 추가 |
| `slack_list_channels` | 접근 가능한 채널 목록 조회 |
| `slack_get_thread` | 스레드 전체 읽기 |

### 컨텐츠 처리

| 도구 | 설명 |
|------|------|
| `slack_upload_snippet` | 코드/로그를 파일로 업로드 (길이 제한 없음) |
| `slack_send_code` | 코드 블록 전송 (syntax highlight, 긴 코드는 자동 파일 업로드) |

### 명령 루프

| 도구 | 설명 |
|------|------|
| `slack_command_loop` | Slack에서 사용자 명령 대기 루프 (채팅 대체 핵심 도구) |

### 에이전트 팀 관리

| 도구 | 설명 |
|------|------|
| `slack_team_create` | 팀 전용 채널 생성 + 초기 멤버 등록 |
| `slack_team_register` | 기존 팀에 새 멤버 추가 |
| `slack_team_send` | 에이전트가 역할 이름/아이콘으로 메시지 전송 |
| `slack_team_read` | 팀 채널 메시지 읽기 (sender 필터 가능) |
| `slack_team_wait` | 팀 채널에서 새 메시지 대기 (sender/keyword 필터) |
| `slack_team_thread` | 팀 스레드 읽기/답장 |
| `slack_team_status` | 팀 현황/멤버 목록 조회 |
| `slack_team_broadcast` | 팀 전체 브로드캐스트 |
| `slack_team_close` | 채널 아카이브 + 최종 요약 |

### 상태 관리

| 도구 | 설명 |
|------|------|
| `slack_save_state` | 루프/팀 상태를 파일에 저장 (세션 복구용) |
| `slack_load_state` | 저장된 상태 복원 (compact/재시작 후 복구) |

## 사용 예시

### 1. Slack 원격 제어 (기본)

```
Agent: slack_command_loop()                        # 명령 대기
User (Slack): "에러 원인 찾아줘"
Agent: slack_add_reaction(ts, "eyes")              # 수신 확인
Agent: (코드 분석 수행)
Agent: slack_reply_thread(ts, "원인: ...")          # 결과 보고
Agent: slack_add_reaction(ts, "white_check_mark")  # 완료
Agent: slack_command_loop(since_ts=ts)              # 다음 명령 대기
```

### 2. 피드백 대기

```
Agent: slack_send_message("작업 완료. 검토 부탁드립니다.")
Agent: slack_wait_for_reply(timeout=120)            # 사용자 피드백 대기
Agent: (피드백 수신 후 반영)
```

### 3. 에이전트 팀 워크플로우

```
── 팀 생성 ──
slack_team_create(
  team_id: "T12",
  team_name: "Feature X 구현",
  members: [
    { id: "lead", role: "lead", agent_type: "planner" },
    { id: "sub-leader-A", role: "sub-leader", agent_type: "planner", track: "A" },
    { id: "worker-A", role: "implementer", agent_type: "implementer", track: "A" },
    { id: "sub-leader-B", role: "sub-leader", agent_type: "planner", track: "B" },
    { id: "worker-B", role: "implementer", agent_type: "implementer", track: "B" }
  ]
)

── 작업 진행 (각 에이전트가 역할 이름으로 보고) ──
slack_team_send(team_id: "T12", sender: "worker-A", message: "구현 완료", update_status: "done")

── 브로드캐스트 (lead → 전체) ──
slack_team_broadcast(team_id: "T12", sender: "lead", message: "Track A 완료. Track B 시작 가능.")

── 팀 종료 ──
slack_team_close(team_id: "T12", summary: "기능 구현 완료. 15개 테스트 통과.")
```

**Slack에서 사용자가 보는 모습:**

```
#team-t12 채널:
  🚀 팀 T12 활성화: Feature X 구현
  멤버 (5명):
    👑 lead — planner
    🎯 sub-leader-A — planner [Track A]
    🔨 worker-A — implementer [Track A]
    🎯 sub-leader-B — planner [Track B]
    🔨 worker-B — implementer [Track B]

  🔨 *worker-A* [A] ✅
  구현 완료

  📢 *[BROADCAST]* 👑 *lead*
  Track A 완료. Track B 시작 가능.

  🎉 팀 T12 작업 완료
  요약: 기능 구현 완료. 15개 테스트 통과.
```

### 4. 위험 명령 승인

```
Agent가 `git push origin main` 실행 시도
  ↓
Slack에 승인 요청 메시지 게시:
  ⚠️ Approval Required
  ```git push origin main```
  ✅ react to approve  |  ❌ react to deny
  ⏳ auto-deny in 120s
  ↓
사용자가 ✅ 리액션 → 명령 실행
사용자가 ❌ 리액션 → 명령 차단
```

## 긴 메시지 자동 처리

MCP 서버는 메시지 길이에 따라 최적의 전송 방식을 자동 선택합니다:

| 길이 | 처리 방식 |
|------|-----------|
| ~3,900자 | 일반 메시지로 전송 |
| 3,900~8,000자 | 여러 메시지로 분할 전송 |
| 8,000자~ | Slack 파일(snippet)로 업로드 |

## 환경 변수

| 변수 | 필수 | 설명 |
|------|------|------|
| `SLACK_BOT_TOKEN` | ✅ | Slack Bot User OAuth Token (`xoxb-...`) |
| `SLACK_DEFAULT_CHANNEL` | 권장 | 기본 채널 ID (`C0123456789`) |
| `SLACK_BOT_USER_ID` | - | 봇 User ID (미설정 시 자동 감지) |
| `SLACK_APPROVAL_TIMEOUT` | - | 승인 훅 타임아웃 초 (기본: 120) |

## 상태 관리

서버는 `state.json`에 다음 상태를 저장합니다:

- **루프 상태**: 활성 여부, 채널, 마지막 처리 ts, 작업 컨텍스트
- **팀 레지스트리**: 팀 ID, 채널, 멤버 목록 및 상태

compact 또는 프로세스 재시작 후 `slack_load_state`를 호출하면 이전 상태로 복구됩니다.

## Claude Code 커스텀 커맨드 (선택)

프로젝트의 `.claude/commands/` 디렉토리에 아래 파일을 추가하면 슬래시 커맨드를 사용할 수 있습니다.

### `/slack-loop`

Slack 명령 대기 루프 모드에 진입합니다. 사용자가 Slack에 입력하는 명령을 읽고, 실행하고, 결과를 스레드로 보고하는 과정을 반복합니다.

- compact/재시작 후 자동으로 이전 상태를 복구
- `종료`, `exit`, `quit`, `stop` 명령으로 루프 종료

### `/slack-team-resume`

compact 또는 재시작 후 에이전트 팀 조율을 복구합니다.

- Slack 채널 메시지 히스토리를 진실의 원천(source of truth)으로 사용
- sub-agent가 독립적으로 계속 실행되는 경우에도 상태를 올바르게 추적

## 문제 해결

| 증상 | 해결 |
|------|------|
| `channel_not_found` | 채널 ID 확인 (이름이 아닌 `C...` 형식의 ID 필요) |
| `not_in_channel` | Slack에서 `/invite @봇이름` 실행 |
| `missing_scope` | Slack App에 필요한 Bot Token Scope 추가 후 재설치 |
| `invalid_auth` | `SLACK_BOT_TOKEN` 값 확인 |
| 팀 채널 생성 실패 | `channels:manage` scope 확인 |
| 에이전트 이름/아이콘 미표시 | `chat:write.customize` scope 확인 |
| 파일 업로드 실패 | `files:write` scope 확인 |
| 승인 훅 리액션 감지 안됨 | `reactions:read` scope 확인 |

## 라이선스

MIT
